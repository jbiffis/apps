# Copy to .env on each environment (dev/prod) and fill in real values.
# .env is gitignored — never commit it.

# Beer admin page password
ADMIN_PASSWORD=

# Tracker app — see tracker/docs/PRIVACY.md
TRACKER_DB_PASSWORD=
TRACKER_JWT_SECRET=  # >=32 chars, long random string. Used to sign JWTs.

# Simgolf (existing) — set via ./.env loaded by docker compose env_file
