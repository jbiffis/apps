services:
  web:
    build: .
    ports:
      - "8081:80"
    volumes:
      - /var/lib/docker/volumes/apps/beer:/var/www/data/beer
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}
